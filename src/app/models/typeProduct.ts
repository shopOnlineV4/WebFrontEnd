import { Size } from './size';
import { Colors } from './color';

export class TypeProduct {
  id: string;
  sizeId: number;
  colorId: number;
  productId : string;
  size : Size;
  colorCode : Colors;
}
